#include "uart.h"
#include "print.h"

u8 fun testAsmFunction(u8 n) asm
{
    lb t0, 8(fp)
    mul t0, t0, t0
    mv a0, t0
}

void fun main()
{
    uart_init();
	uart_putc(65);
    u8 n = 0;
    while(n < 20)
    {
        printNum(testAsmFunction(n));
        n = n + 1;
    }
}