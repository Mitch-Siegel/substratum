//#include "print.h"
//#include "uart.h"

void fun uart_putc(u8 c)
{
    u8 *uart0 = 268435456;
    // 
    while (((*(uart0 + 5)) & (1 << 5)) == 0)
    {
    }

    *(uart0) = c;
}

void fun main()
{
	uart_putc(65);
}