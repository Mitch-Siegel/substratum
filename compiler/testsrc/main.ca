#include "uart.h"
#include "print.h"

u32 fun fib(u8 n)
{
    if(n > 1)
    {
        return fib(n - 1) + fib(n - 2);
    }
    else
    {
        return n;
    }
}

void fun main()
{
    uart_init();
	uart_putc('@');
}