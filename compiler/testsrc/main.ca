#include "print.h"

u32 nRecursions = 0;
fun fibRec(u8 n->u32)
{
	nRecursions += 1;
	if(n < 2)
	{
		return n;
	}
	else
	{
		return fibRec(n - 1) + fibRec(n - 2);
	}
}


fun main(->void)
{
	printStr("hello", 1);

	printNum(123456 + 654321);
}