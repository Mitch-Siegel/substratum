#include "tests-common.sb"

fun main()
{
    for(u8 i = '0'; i <= '9'; i += 1)
    {
        uart_putc(i);
    }
    uart_putc('\n');

    u8 j;
    for(j = '0'; j <= '9';)
    {
        uart_putc(j);
        j += 2;
    }
    uart_putc('\n');

    exit();
}
