#include "tests-common.sb"

class Counter {
    u32 count;
};

impl Counter {
    fun Increment(Counter *self) {
        self->count += 1;
    }

    fun Set(Counter *self, u32 value) {
        self->count = value;
    }
}


fun main() {
    Counter c;
    c.Set(123);
    printNum(c.count);
    c.Increment();
    printNum(c.count);
    exit();
}
